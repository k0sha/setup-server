# Конфиг с секретными данными (пароль, SSH-ключ, SECRET_KEY)
setup-server.conf

# Логи
*.log

# Резервные копии скрипта (создаются при обновлении)
setup-server.sh.bak.*

# Маркер первого запуска
.check_update

# Итоговый отчёт деплоя (содержит IP и EMAIL — не критично, но лучше не пушить)
deploy-info.txt

# macOS
.DS_Store
